const request = Promise.resolve(true);

export default {
  electron: {
    async loginRoom () {
      return await request
    },
    async init () {
      return await request
    }
  },
  web: {
    async loginRoom () {
      return await request
    },
    async init () {
      return await request
    }
  }
}
